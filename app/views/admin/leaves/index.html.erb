</div>
<body>
<%= render 'errors', resource: @leave %>

<div class="container-fluid">
  <div class="row">

    <%= render './layouts/sidebar' %>

    <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-4">
      <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h2 class="h2" id="sayHello">Pending requests</h1>
        <div class="mb-2 mb-md-0">
          <!-- New action modal with new button -->
          <%= render './layouts/new' %>
        </div>
      </div>
      <p>
        <%= select_tag(:status, options_for_select([["Pending", @leave.status == false ],["Approved", @leave.status == true ]], params[:status] ), class:"select") %>
      </p>

        <div class="container_leaves">
          <%= render @leaves %>
        </div>

        <div id="leave-modal" class="modal fade"></div>
    </main>
  </div>
</div>

<!-- Icons -->
<script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
<script>
  feather.replace()
</script>

</body>
